# Local Models

This directory is for local trained classifier artifacts, such as:

- `learned_light.joblib`
- `learned_heavy.pt`

Model files are intentionally ignored by git because they can be large, machine-specific, and reproducible from tagged music libraries. Share them through releases or external artifacts only when that is intentional.
